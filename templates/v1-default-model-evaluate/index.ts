import { UniDataset, ModelEvaluateType, UniModel, EvaluateResult } from '@pipcook/pipcook-core';

const MyModelEval: ModelEvaluateType = async (dataset: UniDataset, uniModel: UniModel): Promise<EvaluateResult> => {
  // write your code
};

export default MyModelEval;
