import { ModelTrainType, UniModel, UniDataset, ModelTrainArgsType } from '@pipcook/pipcook-core';

const MyModelTrain: ModelTrainType = async (dataset: UniDataset, model: UniModel, args: ModelTrainArgsType): Promise<UniModel> => {
  // write your code
};

export default MyModelTrain;
